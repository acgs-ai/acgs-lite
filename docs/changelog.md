<!-- This page mirrors CHANGELOG.md at the package root, which supplies the H1. -->
<!-- Updated automatically by the release pipeline (git-cliff). -->
<!-- The path is resolved against `base_path` in mkdocs.yml (the config dir), so -->
<!-- it works in a standalone checkout and inside the ACGS monorepo alike. -->
--8<-- "CHANGELOG.md"
