# AMD GAIA governance seam

GAIA's optional governance layer is a Protocol. This example builds the four
objects `GaiaGovernanceAdapter` composes, without installing GAIA.

```bash
PYTHONPATH=src python examples/gaia_amd/main.py
```

Expected: `search=ALLOW`, `mail=REVIEW`, `shell=BLOCK`.
